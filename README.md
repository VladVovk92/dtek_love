This Script uses Playwright to access outage schedule DIRECTLY from DTEK website. Kyiv oblast'.

Options:
- -test (yes/true); perform testing
- -gpv (1.1,1.2 ...); select your queue
- -url; correct url is set by default, but why not
- -tomorrow (yes/true); data contains schedule for current day and next day. This switches to next day.
